# 📝 Diary Journal Web App

A simple, browser-based diary journal built using **HTML, CSS, and JavaScript**.  
This application allows users to write daily journal entries, save them by date, and return to previous dates with entries preserved using the browser’s `localStorage`.

---

## ✨ Features

- 📅 Select a date to create or view a diary entry
- 🏷️ Add a title for each entry
- 📝 Write and save daily journal text
- 💾 Entries persist using `localStorage`
- 🔁 Automatically loads saved entries when revisiting a date
- 🎨 Simple and clean user interface

---

## 🛠️ Technologies Used

- **HTML** – page structure
- **CSS** – styling and layout
- **JavaScript** – logic, event handling, and data persistence
- **localStorage API** – client-side data storage

---

## 📂 Project Structure

